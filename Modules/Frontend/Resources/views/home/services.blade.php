<div class="parent-sticky" id="services">
    <div class="sticky-section z-2">
        <div class="scroll-sections">
            <div class="w-100">
                @if (Locales::getCode() == 'ar')
                    <img src="{{ asset('assets/img/scroll-y-ar.svg') }}" alt="">
                @else
                    <img src="{{ asset('assets/img/scroll-y-en.svg') }}" alt="">
                @endif
            </div>
        </div>
    </div>
</div>
